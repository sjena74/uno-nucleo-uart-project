# daily log

## 2026-07-29 - HC-SR04 Ultrasonic Bring-Up (wiring and firmware via online arduino library)

### Next Steps
- HC-SR501 PIR sensor alongside ultrasonic on same breadboard
update 7/29 evening
- *all sensors complete!*
- next time, start to bring up actuators.
update 7/29 evening
- HC-SR501 PIR sensor bringup completed
- https://www.youtube.com/watch?v=EqWxUT4o8kA (referred for changing sensitivity and time delay via physical interface of sensor)

- Also completed photoresitor wiring with pull-down resistor.
### Challenges 
- PIR sensor was reading not as frequently as I wanted it to. Fix: moved on-board jumper from single-trigger to repeat-trigger

## 2026-07-30 

### Update
- wired LED with pull-up resistor 
- watched https://www.youtube.com/watch?v=jXEVZga9l1s (on wiring up relay)
- relay bring-up completed with an additional diode for load safety
  - testing works, clicks every 1 second


## 2026-08-07

### Update
- Wired Servo SG90 (pin 7)
- All three actuators are working in one program

### Challenge
- Servo.h not found even when tried to build
- FIX: added the literal url to lib_deps in .ini

## Next Steps
- Will add in the last piece LCD1602

## 2026-08-09

### Update 
- Wired LCD1602 to board using datasheet

### Challenge
- had to find some extra male-female connectors since nodes from board to uno are far

## Next Steps
- Will add firmware for LCD1602 for simple bringup


## 2026-08-16

### Update
- Added LCD1602 firmware ("Hello Sohan" as a default for now)
- Tested and worked using LiquidCrystal Library
- All components working together!

### Challenge
- had to figure out how to use a potentiometer to fade/unfade the text from the LCD
- fix: researched to figure out that instead of hardcoding a fixed voltage which could/could not work, many people used a potentiometer to tune the voltage input to the LCD as they liked.

## Next Steps


## 2026-08-18
### Update
- Replaced fixed-delay loop with sensor-driven logic:
  - If object < 10cm away, relay + LCD "Object Near" + servo moves 90 degrees
  - if light reaches a light threshold, LED on
- works end-to-end

### Challenge
- had to figure out how to group tasks and display information on serial monitor in a nice form
- Phase 2 is done!
- Redesign the demo loop with real sensor-driven logic

## Next Steps
- Will start to enable the UART protocol on UNO side using poll-response

## 2026-08-19
-Implemented full packet protocol on uno: poll-response for sensors (0x01 - 0x03) and command handling for actuators (0x10-0x13)
- Built python test harness (pyserial) to simulate nucleo and verify manually
- verified checksum math correct on real packet sensors and verified command packet correctly received and executed (via LED)


## Next Steps
- Bare-metal USART1 on STM32 setup
- test with tx-rx loopback jumper on nucleo before connecting to uno

## 2026-08-20
-Wired resistor divider (1k/2k) uno Tx -> divider -> Nucleo PA10
- Wired nucleo tx -> uno rx
- tied uno and nucleo grounds together
- flashed poll-test firmware to Nucleo, verified via LD2: send 0x01 poll, recieves valid 0xAA start byte back from UNO

## Next Steps:
- start packet parse on nucleo
- implement decision logic on nucleo

## 2026-08-20 (part 2)
- full packet parsing + validation on nucleo
- implemented decision making on nucleo: polls 3 sensors sequentially, sends commands based on thesholds reached
- Nucleo is the decision-maker while uno is purely input/output

#### SYSTEM COMPLETE


